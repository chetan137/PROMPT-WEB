<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Prompt Engineering</title>
    <link rel="stylesheet" href="styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            overflow-x: hidden;
        }

        body {
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #710d2f, #1f2251);
        }

        /* Navbar */
        .navbar {
            padding: 10px 20px;
            text-align: center;
            background-image: linear-gradient(to right, #434343 0%, black 100%);
        }

        .name1 {
            margin-left: 600px;
            font-weight: bold;
        }

        .navbar h2 {
            color: #fff;
            font-size: 24px;
        }

        .other {
            display: flex;
            justify-content: center;
            gap: 30px;
            padding: 10px;
            font-size: 18px;
            font-weight: bold;
            background-image: linear-gradient(to right, #434343 0%, black 100%);
        }

        .other a {
            color: white;
            text-decoration: none;
            transition: color 0.3s;
        }

        .other a:hover {
            color: #f69d3c;
        }

        .loginuser {
            position: relative;
            margin: 1rem;
            text-align: center;
        }

        .icon img {
            border-radius: 50%;
            height: 80px;
            width: 80px;
            margin-top: 3rem;
        }

        footer {
            height: 50px;
            text-align: center;
            padding: 10px;
            margin-top: auto;
            background: #333;
            color: white;
            font-weight: bold;
            font-style: italic;
            background-image: linear-gradient(to right, #434343 0%, black 100%);
        }

        .welcome {
            text-align: center;
            margin-top: 60px;
            animation: textGlow 3s ease-in-out infinite alternate-reverse;
        }

        .greeting {
            font-size: 30px;
            font-weight: bold;
            font-style: italic;
        }

        .welcome {
            height: 150px;
            overflow: hidden;

        }

        .welcome h1 {
            white-space: nowrap;
        }

        #element {
            display: inline-block;
            min-width: 200px;
            height: 50px;
            line-height: 50px;
            white-space: nowrap;
        }

        @keyframes textGlow {
            from {
                text-shadow: 0 0 10px #f69d3c, 0 0 20px #f69d3c;
            }

            to {
                text-shadow: 0 0 20px #ffb84d, 0 0 40px #ffb84d;
            }
        }

        .card {
            color: black;
            background-color: white;
            padding: 20px;
            border-radius: 20px;
            width: 30%;
            margin: 10px;

            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s, background-color 0.3s;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.5);
            background-color: #f69d3c;
        }

        .showin {

            margin-left: 550px;
        }

        .form-control {
            max-width: 600px;
            margin: 0 auto;

        }

        textarea {
            height: 90px;

        }

        .section {
            padding: 50px;
            text-align: center;
        }

        .learn-cards {
            display: flex;
            margin-top: 30px;
            flex-wrap: wrap;
            justify-content: space-around;
        }

        @media (max-width: 768px) {
            .other {
                flex-direction: column;
                align-items: center;
            }

            .other a {
                margin: 5px 0;
            }
        }

        @media (max-width: 480px) {
            .navbar h2 {
                font-size: 20px;
            }

            .card {
                width: 90%;
            }

            .form-control {
                width: 90%;
            }
        }

        /* .small-circle {
            width: 20px;
            height: 20px;
            background-color: #ff4c4c;
            border-radius: 50%;
            position: absolute;

            left: 200px;
            animation: moveSmall 12s infinite alternate ease-in-out;
        } */
    </style>
</head>

<body>

    <nav class="navbar">
        <h2 class="name1">AI Prompt Engineering</h2>
    </nav>

    <div class="other">
        <a href="/">Home</a>
        <a href="/AItools">AI Tools</a>
        <a href="/team">Team</a>
        <a href="/feed">Feedback</a>
        <a href="/contact">Contact us</a>
    </div>

    <!-- <div class="small-circle"></div> -->


    <div class="loginuser">
        <div class="icon"><img src="images/login.png" alt="User Icon"></div>
    </div>


    <div class="welcome">
        <h1 id="element" class="name"></h1>
        <p class="greeting">Welcome to the course</p>
    </div>


    <div class="divmain row">
        <div class="card showin">
            <h5 class="card-header">Round <%= question.round %>
            </h5>
            <div class="card-body">
                <h5 class="card-title">Question</h5>
                <p id="promptText" class="card-text">
                    <b>
                        <%= question.questionText %>
                    </b>
                </p>
            </div>
        </div>
        <br>
        <hr><br><br>
        <div>
            <form class="form-control" action="/questions/check" method="POST">
                <input type="hidden" name="questionId" value="<%= question._id %>">
                <input type="hidden" name="userId" value="<%= userId %>">
                <label for="userPrompt">Your Prompt:</label>
                <textarea class="form-control" id="userPrompt" name="userPrompt" required rows="4"></textarea>
                <button type="submit" class="btn btn-primary mt-3">Submit</button>
            </form>
        </div>
    </div>


    <section id="about" class="section">
        <h1>About the Course</h1>
        <p>This course will guide you through the basics of prompt engineering, helping you create more effective
            prompts for AI models.</p>
    </section>


    <section id="learn" class="section">
        <h2>What You'll Learn</h2>
        <div class="learn-cards">
            <div class="card">
                <h3>Understanding Prompts</h3>
                <p>Get to know what prompts are and how they influence AI behavior.</p>
            </div>
            <div class="card">
                <h5 class="card-header">Round
                    <span id="roundText"></span> <!-- Use an empty span for typing effect -->
                </h5>
                <div class="card-body">
                    <h5 class="card-title">Question</h5>
                    <p id="promptText" class="card-text">
                        <b>
                            <span id="questionText"></span> <!-- Use an empty span for typing effect -->
                        </b>
                    </p>
                </div>
            </div>


            <div class="card">
                <h3>Advanced Techniques</h3>
                <p>Dive into advanced prompt engineering techniques to refine your skills.</p>
            </div>
        </div>
    </section>


    <footer>
        <p>&copy; Designed and developed by Ashish and Chetan. All rights reserved.</p>
    </footer>

    <!-- JavaScript for Typed.js -->
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
    <script>
        // Initialize Typed.js for the welcome text
        var typed = new Typed('#element', {
            strings: ['Hello Participant...🤖'],
            typeSpeed: 50,
            backSpeed: 25,
            loop: true
        });

        // Example: Modify the prompt text after the page loads
        document.addEventListener('DOMContentLoaded', () => {
            const promptText = document.getElementById('promptText');
            promptText.innerText += ' - This is your optimal prompt for the task.';
            let sanitizedQuestion = promptText.replace(/[^\w\s*']/g, '');

            sanitizedQuestion.innerText = sanitizedQuestion.innerText.toUpperCase();
        });

        // document.addEventListener('mousemove', (e) => {
        //     const circle = document.querySelector('.small-circle');
        //     circle.style.left = `${e.clientX - 10}px`;  // Adjusted to center the circle on the mouse
        //     circle.style.top = `${e.clientY - 10}px`;
        // });
    </script>
</body>

</html>
